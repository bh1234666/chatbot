# ─────────────────────────────────────────────────────────────────────────
# 可选 / 外部依赖。核心服务不装也能启动(相关功能降级或禁用)。
# ─────────────────────────────────────────────────────────────────────────

# PostgreSQL 后端 —— 当前 db/pool.py 的 PG 分支【未实现】,装了也不会被使用。
# 真要上 PG 时再启用,并实现 pool.py 的 asyncpg 分支。
# asyncpg>=0.29

# MinerU OCR 引擎 —— 由 main.py 作为独立后台子进程拉起(mineru/bg_service.py)。
# 安装方式见 MinerU 官方文档;import mineru 仅在 OCR 路径触发。
# mineru
