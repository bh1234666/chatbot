# -*- coding: utf-8 -*-
# ─────────────────────────────────────────────────────────────────────────
# 运行时核心依赖。
# 由 `tools/scan_imports.py` 对全工程 import 静态扫描得出(见 import 名 → PyPI 名映射)。
# 版本下限按 2026-05 工程实际使用的 API 保守约束;按需在部署环境锁定具体版本。
# ─────────────────────────────────────────────────────────────────────────

# ── Web 框架 / 服务 ──
fastapi>=0.110
uvicorn[standard]>=0.29        # ASGI runner(代码未直接 import,但 main:app 靠它启动)
sse-starlette>=2.0             # import sse_starlette —— SSE 流式响应

# ── 配置 ──
pydantic>=2.6
pydantic-settings>=2.2         # import pydantic_settings —— config.Settings
python-dotenv>=1.0             # import dotenv

# ── LLM / HTTP ──
openai>=1.30                   # AsyncOpenAI 客户端
httpx>=0.27

# ── 数据库 ──
aiosqlite>=0.20                # 默认 SQLite 后端

# ── ID ──
python-ulid>=2.2               # import ulid; 用法 ulid.ULID()

# ── 文档处理(office / pdf 工具链)──
python-docx>=1.1               # import docx
python-pptx>=0.6.23            # import pptx
openpyxl>=3.1                  # xlsx
pypdf>=4.0
PyMuPDF>=1.24                  # import fitz
lxml>=5.0

# ── 图像 / 数值(OCR、媒体元数据、绘图)──
Pillow>=10.0                   # import PIL
numpy>=1.26
matplotlib>=3.8
